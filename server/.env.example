# MongoDB Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority&appName=AppName

# JWT Configuration - IMPORTANT: Generate a strong random secret for production
JWT_SECRET=your-super-secure-random-jwt-secret-at-least-32-characters-long

# Server Configuration
PORT=3001
NODE_ENV=development

# Security Note: 
# - Never commit real credentials to version control
# - Generate a strong, random JWT_SECRET using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# - Use environment variables in production deployment